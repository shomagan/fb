/* ФБ "Преобразователь в массив" */

typedef struct {
	Register_type Input[256];    // Входные элементы
} fb00060_IN_type;


typedef struct {
	Register_type Output[256];  // Выходной массив
} fb00060_OUT_type;
